# OpenClaw GCP Deployment — Environment Variables
# Copy to terraform/terraform.tfvars and fill in values.
# Never commit terraform.tfvars to version control (it's in .gitignore).

# ── Required ──────────────────────────────────

# GCP project ID (find in GCP Console → Project Selector)
project_id = "your-gcp-project-id"

# Telegram Bot Token from @BotFather → /newbot
telegram_bot_token = "<your-telegram-bot-token>"

# Comma-separated Telegram user IDs of family members
# Get your ID: message @userinfobot on Telegram
allowed_user_ids = "123456789,987654321,456789123"

# ── LLM API Keys (at least one required) ──────

# Groq — free, fast, Llama 3.3 70B — recommended starting point
# Sign up: https://console.groq.com
groq_api_key = "gsk_..."

# Google Gemini — free tier (15 RPM, 1M tokens/day)
# Get key: https://aistudio.google.com/app/apikey
gemini_api_key = "AIza..."

# Anthropic Claude — pay-per-use, no free tier
# Sign up: https://console.anthropic.com
anthropic_api_key = ""

# ── Optional ──────────────────────────────────

# GCP region (default: us-central1)
# region = "us-central1"
